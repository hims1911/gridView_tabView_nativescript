# gridView_tabView_nativescript
grid view and tabs view created in native script.

first step is to do npm install
`npm install`
in the root directory.

then simply run preview command
`tns preview`

